Run ruff on the calcine codebase and fix any issues.

```bash
uv run ruff check --fix .
```

After fixing, report what was changed. If any errors could not be auto-fixed, explain each one and fix it manually.
